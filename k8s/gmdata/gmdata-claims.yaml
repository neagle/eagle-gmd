apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gmdata-pvc
  namespace: gmdata
spec:
  accessModes:
    - ReadWriteOnce
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  # Use the default
  # storageClassName: 

